server:
  port: 8080

jwt:
  secret: YourSecretKeyHereMustBeLongEnoughForHS512AlgorithmAtLeast64Characters123456

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
          lower-case-service-id: true
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins: "*"
            allowed-methods: "GET,POST,PUT,DELETE,OPTIONS,PATCH"
            allowed-headers: "*"
            allow-credentials: false
      routes:
        - id: auth-service
          uri: http://auth-service:8082
          predicates:
            - Path=/api/auth/**
        - id: question-service
          uri: http://question-service:8083
          predicates:
            - Path=/api/questions/**
        - id: quiz-service
          uri: http://quiz-service:8084
          predicates:
            - Path=/api/quiz/**
        - id: notification-service
          uri: http://notification-service:8085
          predicates:
            - Path=/api/notify/**

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: never

logging:
  level:
    org.springframework.cloud.gateway: WARN
    reactor.netty: WARN